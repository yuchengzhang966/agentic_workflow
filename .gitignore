# Python
.venv/
__pycache__/
*.pyc

# Local SQLite checkpoint store (runtime state — not source)
*.db
checkpoints.db

# Node / Vite
node_modules/
dist/
*.tsbuildinfo

# Env (real secrets — never commit)
.env
.env.local
backend/.env
backend/.env.local
frontend/.env
frontend/.env.local

# misc
.DS_Store
